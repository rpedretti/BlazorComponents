@page "/counter"
@inherits CounterPageBase

<Counter Title="@CounterTitle"
         ButtonText="@CounterIncrementDescription"
         IncrementAmount="@IncrementAmount"
         CurrentCount="CurrentCount" 
         CurrentCountChanged="@CounterChanged"/>
